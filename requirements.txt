# DR + DME 多任务识别系统 — pip 依赖(Python 3.14 友好版)
#
# ⚠️ Python 3.14 注意:
#   - numpy 的 3.14 wheel 需 >=2.3.2(老的 1.26 分支无 3.14 包,会触发源码编译而失败)。
#   - PyTorch cp314 wheel 自 torch 2.10 起提供;但部分 CUDA 版本的 cp314 GPU wheel 可能尚未上架,
#     有时只装到 CPU 版。装完务必验证:
#       python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
#     若为 False / 版本带 +cpu,说明该 CUDA 没出 cp314 GPU 包,需换 CUDA 版本或退回 Python 3.12。
#
# 安装步骤:
#   1) 先装 PyTorch(按 CUDA 版本,从官方 https://pytorch.org/get-started/locally/ 取命令),例如:
#        pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#      让 pip 自行解析与你 Python/CUDA 匹配的最新 cp314 构建。
#   2) 再装其余依赖:
#        pip install -r requirements.txt

# ---- 深度学习(若已按上面第 1 步装好可忽略这两行的下界提示)----
torch>=2.12          # cp314 wheel 需 >=2.10,建议最新
torchvision>=0.23    # 与所装 torch 配套(随 torch 一起装最稳)
timm>=1.0            # convnext_tiny + 预训练权重(纯 Python,无 cp314 限制)

# ---- 数据 / 科学计算 ----
numpy>=2.3.2         # 3.14 wheel 的最低版本
pandas>=2.2
scipy>=1.14
scikit-learn>=1.5    # QWK = cohen_kappa_score(weights="quadratic")

# ---- 图像处理 / 增广 ----
opencv-python-headless>=4.11   # 圆裁剪 / Ben Graham 归一化(放宽以取 cp314 wheel)
albumentations>=1.4,<2.0       # 纯 Python;锁 1.x 避免 2.0 的 API 变更
Pillow>=10.4

# ---- 方法实现 ----
coral-pytorch>=1.4   # 纯 Python(本仓库 losses/ordinal.py 也内置了等价实现,可不依赖它)
grad-cam>=1.5        # Grad-CAM (import pytorch_grad_cam)

# ---- 读取 Messidor 标注 Excel(convert_messidor_labels.py)----
xlrd>=2.0            # 读旧式 .xls(官方 Messidor 标注)
openpyxl>=3.1        # 读 .xlsx

# ---- 配置 / 日志 / 可视化 ----
pyyaml>=6.0
tqdm>=4.66
tensorboard>=2.17
matplotlib>=3.9
seaborn>=0.13
